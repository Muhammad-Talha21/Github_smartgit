Add a.txt
checking in smartgit
after commmiting in smartgit we push so it will also come onto git hub
and if from server smartgit will automatically sync the data, which tell there is some change and then we will pull
